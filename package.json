{
  "name": "mtg-calc",
  "private": true,
  "description": "Repo root. Holds the end-to-end suite, which tests the built artifact rather than any one package -- the frontend keeps its own package.json and its own unit tests.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
